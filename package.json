{
	"devDependencies": {
		"lerna": "^2.11.0"
	},
	"scripts": {
		"postinstall": "lerna bootstrap && lerna run prepare --scope !demo",
		"test": "lerna run test"
	},
	"private": true,
	"workspaces": [
		"packages/*"
	]
}
